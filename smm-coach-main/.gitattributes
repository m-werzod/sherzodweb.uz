# Prisma migration files are content-hashed (Prisma stores a checksum in
# _prisma_migrations). Force LF so the hash is identical on Windows + Linux and
# `migrate deploy` never warns "migration was modified after applied".
packages/db/prisma/migrations/**/*.sql text eol=lf
packages/db/prisma/migrations/**/migration_lock.toml text eol=lf
